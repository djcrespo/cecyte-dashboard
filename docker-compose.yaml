version: '3'

services:
  cecyte-dash:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cecyte-dashboard
    restart: unless-stopped
    ports:
      - 3000:3000
    networks:
      - default
    environment:
      - VIRTUAL_HOST=cecyte.cosvion.com
      - LETSENCRYPT_HOST=cecyte.cosvion.com
      - VIRTUAL_PORT=3000
networks:
  default:
    external:
      name: net-proxy